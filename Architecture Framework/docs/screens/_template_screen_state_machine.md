# Screen State Machine: <ScreenName>

## Artifact ID
**Format:** `SCREEN-<number>` или `SCREEN-<screen-name>` (например, `SCREEN-001` или `SCREEN-HOME`)  
**ID:** `SCREEN-<number>`

## JSON Schema
Для валидации структуры документа используйте JSON Schema: [`docs/schemas/state_machine.schema.json`](../schemas/state_machine.schema.json)

## Traceability
**Обязательно заполнить все поля трассировки для связи с другими артефактами.**

- **Feature ID:** `FEATURE-<id>` (обязательно)
- **Screen ID:** `SCREEN-<id>` (обязательно, должен совпадать с Artifact ID)
- **Related Use Cases:** `<use-case-name>` (список use cases, реализуемых на экране)

<!-- ИНСТРУКЦИЯ ПО ЗАПОЛНЕНИЮ
Цель: Описать детерминированное поведение экрана через состояния и переходы. State Machine делает поведение UI предсказуемым, тестируемым и понятным для ИИ.

Когда заполнять: Для сложных экранов на Этапе 2.2. Создавайте State Machine для экранов с множеством состояний (loading, error, empty, content, refreshing) или сложной логикой переходов.

Исходные данные:
- Feature Spec (раздел "Экраны и состав UI")
- Дизайн-макеты с состояниями (loading, error, empty, content)
- Требования к поведению при ошибках
- Требования к обновлению данных (pull-to-refresh, auto-refresh)

Где брать:
- Feature Spec (шаг 2.1)
- Дизайн-макеты с различными состояниями
- Обсуждения с дизайнерами о UX при ошибках
- Требования к производительности (когда показывать loading)

Советы:
- **Обязательно заполните раздел Traceability** — укажите Feature ID и Screen ID
- Состояния должны быть явными и покрывать все возможные состояния экрана
- Используйте параметризованные состояния (например, Content(data), Error(message))
- Actions/Intents должны быть событиями пользователя или системы, а не UI-компонентами
- Effects должны описывать побочные эффекты (загрузка данных, навигация, аналитика)
- Таблица переходов должна быть полной — каждая комбинация (state, event) должна иметь результат
- Правила деградации должны быть согласованы с Feature Spec (раздел "Политика деградации")
- Минимальный набор тестов должен покрывать все переходы из таблицы
- State Machine должна быть детерминированной — одинаковые (state, event) всегда дают одинаковый результат
-->

## 1. Назначение
Для какого экрана, какие проблемы решает (детерминированность, тестируемость).

## 2. State (состояния)
- `Idle`
- `Loading`
- `Content(...)`
- `Error(...)`
- `Refreshing(...)`
(адаптируйте под экран)

## 3. Actions/Intents (события)
- `OnOpen`
- `OnRetry`
- `OnPullToRefresh`
- ...
(адаптируйте под экран)

## 4. Effects (побочные эффекты)
- `Load...`
- `TrackEvent(...)`
- `OpenRoute(...)`

## 5. Таблица переходов
| Given state | When event | Then state | Effects |
|---|---|---|---|
| | | | |

## 6. Правила деградации
- ...

## 7. Минимальный набор unit-тестов reducer/store
1. ...
