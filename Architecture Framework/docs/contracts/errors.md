# Error Model & UI Policy

<!-- ИНСТРУКЦИЯ ПО ЗАПОЛНЕНИЮ
Цель: Определить единую модель ошибок на уровне Domain и политику их отображения в UI. Это обеспечивает консистентное поведение приложения при ошибках.

Когда заполнять: На Этапе 3.2 один раз для всего проекта. Обновляется при добавлении новых типов ошибок или изменении политики отображения.

Исходные данные:
- API Contracts (раздел "Единая модель ошибок")
- Feature Spec (раздел "Политика деградации")
- Требования к UX при ошибках
- ADR по обработке ошибок (если есть)

Где брать:
- API Contracts (шаг 3.1)
- Feature Spec (шаг 2.1)
- Дизайн-гайдлайны по обработке ошибок
- Обсуждения с дизайнерами

Советы:
- Domain errors должны быть независимы от транспортного уровня (HTTP коды, сетевые ошибки)
- Список Domain errors должен быть полным и покрывать все типы ошибок из API Contracts
- Политика отображения должна быть конкретной — что показываем, какой текст, какие действия доступны
- Retry policy должна быть явной — когда показываем кнопку повтора, когда автоматически повторяем
- CTA (Call To Action) должны быть понятными — куда ведёт кнопка при ошибке
- Логирование и аналитика должны быть согласованы с Observability документом
- Политика должна быть согласована с дизайнерами для консистентного UX
-->

## 1. Domain errors (единый список)
- AuthRequired
- NetworkUnavailable
- Timeout
- ServerError(code, message?)
- ValidationError(fields)
- Unknown(cause)

## 2. Политика отображения в UI
| Domain error | UI behavior | Retry | CTA |
|---|---|---|---|
| AuthRequired | ... | ... | ... |
| NetworkUnavailable | ... | ... | ... |

## 3. Логирование и аналитика ошибок
- Что логируем:
- Что отправляем в аналитику:
