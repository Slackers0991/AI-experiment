# Feature Spec: <FeatureName>

## Artifact ID
**Format:** `FEATURE-<number>` (например, `FEATURE-001`)  
**ID:** `FEATURE-<number>`

## JSON Schema
Для валидации структуры документа используйте JSON Schema: [`docs/schemas/feature.schema.json`](../schemas/feature.schema.json)

## Traceability
**Обязательно заполнить все поля трассировки для связи с другими артефактами.**

- **Related Features:** `FEATURE-<id>` (если фича зависит от других фич)
- **Related Screens:** `SCREEN-<id>` (список всех экранов фичи)
- **Related Use Cases:** `<use-case-name>` (список use cases из раздела 4)
- **Related API Contracts:** `<operation-name>` (список операций API из contracts/api.md)

<!-- ИНСТРУКЦИЯ ПО ЗАПОЛНЕНИЮ
Цель: Детально описать поведение фичи, её экраны, use cases, доменную модель и политики (кеш, деградация, аналитика). Это основной документ для проектирования фичи.

Когда заполнять: Для каждой новой фичи на Этапе 2.1. Обновляется при изменении требований или поведения.

Исходные данные:
- User Stories из бэклога продукта
- Дизайн-макеты экранов (Figma, Sketch)
- Требования к бизнес-логике
- Требования к аналитике
- Требования к производительности

Где брать:
- Product Backlog
- Дизайн-система и макеты
- Встречи с дизайнерами
- Встречи с продуктовой командой
- Существующие аналогичные фичи (если есть)

Советы:
- **Обязательно заполните раздел Traceability** — используйте идентификаторы для связи с другими артефактами
- Раздел "Цель и границы" критически важен — явно укажите, что НЕ входит в фичу
- User Stories должны быть конкретными и измеримыми
- В разделе "Экраны" перечислите все секции/блоки — это поможет при создании State Machine
- Use cases должны быть архитектурными (не UI-действия), описывать бизнес-логику
- Domain model должна быть минимальной — только то, что нужно для фичи
- Политика кеша должна быть конкретной (TTL, стратегия обновления)
- Политика деградации должна покрывать все типы ошибок
- Аналитика должна быть привязана к конкретным действиям пользователя
- Тестовые требования должны быть в формате Given/When/Then для ясности
-->

## 1. Цель и границы
**Цель:**  
**Вне границ:**  

## 2. Пользовательские сценарии (User stories)
- ...

## 3. Экраны и состав UI (секции/блоки)
- Screen: <ScreenName>
  - Sections:
    - ...

## 4. Use cases (архитектурные)
- `...` — цель, вход/выход
- `...`

## 5. Domain model (минимум)
- Основные сущности:
  - ...
- Ошибки (Domain):
  - ...

## 6. Источники данных
- Репозитории (интерфейсы):
  - ...
- Data sources (remote/local):
  - ...

## 7. Политика кеша и обновления
- TTL:
- SWR/offline:
- Refresh:
- Инвалидация:

## 8. Политика деградации (частичные ошибки)
- Что скрываем:
- Что показываем как placeholder:
- Когда экран уходит в глобальный Error:

## 9. Аналитика
- events:
  - name, params, when fired

## 10. Тестовые требования (Given/When/Then)
- ...

